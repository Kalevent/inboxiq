apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dspy-artifacts-pvc
  namespace: kaley
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp2-csi
  resources:
    requests:
      storage: 2Gi
