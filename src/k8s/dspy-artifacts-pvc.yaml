apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dspy-artifacts-pvc
  namespace: kaley
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3-csi
  resources:
    requests:
      storage: 1Gi
