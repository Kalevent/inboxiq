# Terraform state — stored in S3, never in git
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Local variable overrides — may contain secrets
*.tfvars
*.tfvars.json

# Crash logs
crash.log
